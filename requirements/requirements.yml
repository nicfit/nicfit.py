main:
    - deprecation
    - attrs
    - PyYaml >= 4.2b1

extra_cookiecutter:
    - cookiecutter

extra_curio:
    - curio

extra_shell:
    - prompt-toolkit ~= 1.0

test:
    - pytest
    - pytest-cov
    - pytest-runner
    - pytest-asyncio
    - pytest-curio
    - babel

dev:
    - Sphinx
    - sphinx_rtd_theme
    - babel
    - check-manifest
    - flake8
    - ipdb
    - pip-tools
    - tox
    - twine
    - wheel
    - pyaml
    - pss
    - git+https://github.com/nicfit/gitchangelog.git
